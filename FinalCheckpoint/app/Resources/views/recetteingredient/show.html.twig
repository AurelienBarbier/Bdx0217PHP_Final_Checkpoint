{% extends 'base.html.twig' %}

{% block body %}
    <h1>Recetteingredient</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recetteIngredient.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ recetteIngredient.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('recetteingredient_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('recetteingredient_edit', { 'id': recetteIngredient.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
